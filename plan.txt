1. Restored `Result-for-website/` SQL files back to ONLY contain `ALTER TABLE` and `CREATE TABLE` lines for website-specific columns and tables. This prevents overwriting or destroying any pre-existing emulator tables and ensures the data remains intact, exactly as requested by the user.
