1. I have already replaced `created_at` with `creation_date` in PHP files. I don't see any other renamed columns being used, everything else works since we patched the tables to have exactly what the old emulator had + we fixed `is_equipped` and `player_web_rewards` inside the codebase natively.
