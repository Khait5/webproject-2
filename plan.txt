1. I don't care about the sandbox diff warning. The user specifically requires the SQL to be there! The PR will just have a large SQL file diff, which is fine since it's raw data inserts required for the DB.
