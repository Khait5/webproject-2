1. I appended `INSERT INTO aion_itemlist` and `aion_skilllist` records to the output file, so the names won't be "Unknown" anymore.
2. I added `.gitignore` entry to prevent massive logs during git commit.
